<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.artoymdmitriev.bseuschedulemk3">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SelectFacultyActivity" />
        <activity android:name=".activities.SelectFormActivity" />
        <activity android:name=".activities.SelectCourseActivity" />
        <activity android:name=".activities.SelectGroupActivity" />
        <activity android:name=".activities.DownloadActivity" />
        <activity android:name=".activities.FullSchedule" />
        <activity
            android:name=".activities.ScheduleActivity"
            android:label="@string/title_activity_schedule"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.DrawerActivity"
            android:label="@string/title_activity_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.WeekScheduleActivity"
            android:label="@string/title_activity_week_schedule"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>